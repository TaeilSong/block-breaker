{
  "name": "breakout-bot-webapp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "vite --host 127.0.0.1",
    "build": "vite build",
    "test": "node --test",
    "benchmark": "node src/benchmark.js"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
